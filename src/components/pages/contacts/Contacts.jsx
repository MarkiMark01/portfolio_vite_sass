import styles from './contact.module.scss';

const Contacts = () => {
return(
    <section id="contact">
        <h2>Contact me</h2>
        <div className={styles.contactBox}></div>
    </section>
)
}
export default Contacts;